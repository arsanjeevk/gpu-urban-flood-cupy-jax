# GPU benchmark

The required-count secondary benchmark used one identical 60-minute production workload per backend: 3 warm-ups and 10 measured runs, CUDA device 0, and a correctness gate after every run. Raw logs, arrays, CSV observations, and summary statistics are under `../benchmarks/secondary_60min/`. It is not represented as the unexecuted repeated 24-hour official benchmark.
